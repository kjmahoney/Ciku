startQuiz = function(cards){
  //start cube spin
  speed = 0.01
  //create the session Array
  let placeHolderArray = []
  let sessionArray = placeHolderArray.concat(cards)
  return sessionArray
}

// isCorrect = function(userAnswer, actualAnswer, )
